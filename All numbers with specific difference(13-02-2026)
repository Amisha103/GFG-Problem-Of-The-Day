All numbers with specific difference

Given a positive number n and a number d. Find the count of positive numbers smaller or equal to n such that the difference between the number and sum of its digits is greater than or equal to given specific value d.

Examples:

Input: n = 13, d = 2
Output: 4
Explanation:
There are 4 numbers satisfying the
Conditions. These are 10, 11, 12 and 13.
Input: n = 14, d = 3
Output: 5
Explanation:
There are 5 numbers satisfying the
Conditions. These are 10, 11, 12, 13 and 14.
Constraints:
1 ≤ n ≤ 109
1 ≤ d ≤ 109


JAVA-
class Solution {
    public int getCount(int n, int d) {
     int start = 1;
     int end = n;
     while(start <= end)
     {
         int mid = (start + end) / 2;
         if(isPossible(mid, d))
         {
             end = mid - 1;
         }
         else
         {
             start = mid + 1;
         }
     }
     return n - end;
        
    }
    boolean isPossible(int num, int d)
    {
        int sum = 0;
        int dummy = num;
        while(num > 0)
        {
            sum = sum + (num % 10);
            num = num / 10;
        }
        
        return dummy - sum >= d?true:false;
    }
};


PYTHON-
class Solution:
    def getCount(self, n, d):
        start = 1
        end = n
        while start <= end:
            mid = (start + end) // 2
            if self.isPossible(mid, d):
                end = mid - 1
            else:
                start = mid + 1
        
        return n - end
        
    def isPossible(self, num, d):
        sum = 0
        dummy = num
        while num > 0:
            sum = sum + (num % 10)
            num = num // 10
        
        if dummy - sum >= d:
            return True
        return False
